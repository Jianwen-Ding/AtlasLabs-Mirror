# AtlasLabs

AtlasLabs serves mainly as a tool for exploring and developing texture atlas generating algorithms. 

It provides a common interface for texture atlas algorithms, implements adapters for a couple popular texture atlas generation systems, provides benchmarks for those algorithms in a variety of situations, and holds a interface for implementing new algorithms in the backend of the system.